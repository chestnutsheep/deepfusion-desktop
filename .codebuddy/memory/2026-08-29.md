# 2026-08-29 (续)

## 用户授权边界（重要）
- 用户主要负责金融技术指引，代码由 AI Agent 代劳。**明确授权：工程技术我拍板，涉及改核心计算逻辑再问他。**
- 目标：把 `DeepFusion` 后端作为**独立、干净、可复现、可交付、可持续维护**的核心引擎项目；`server`/`Web UI`/`Desktop Panel` 三者作为 DeepFusion 的三个模块各自独立管理。
- 当务之急：后端逻辑工程化清理与标准化。**核心计算逻辑一律不动**。

## 后端现状核查（已确认）
- `deep_fusion/__init__.py` 的 `main()` 是干净的 MCP 引擎入口（`deep-fusion` CLI，支持 `inspect`/`--http`），**不含 daemon 线程**。
- 根 `serve.py` 是带保姆线程的便捷启动器，内含 4 个 `while True` daemon 线程（缓存预热/政策采集/行业市场采集/日报），调用 `deep_fusion` 内部模块。
- `data/` 有 5 文件：butler.db/industry_data.db/market_data.db/reports.db/score_calibration.json，需随包携带；但 `.gitignore` 忽略了 `*.db` 与 `data/*.db` → clone 缺库隐患。
- `pyproject` 锁 `requires-python>=3.14`（多数环境装不上）；`Dockerfile` 用 3.14-slim 且 `COPY DeepFusion/deep_fusion/` 路径错（应为 `/deep_fusion/`）。
- 依赖混乱：pytest-asyncio/pandas-stubs/scipy-stubs 混进运行时。

## 执行计划（按 P0→P1）
1. P0：降 Python 至 >=3.11、Dockerfile 改 3.12 + 修 COPY 路径、依赖分组（测试/stub 移 dev）。
2. P0：调度器剥离——新建 `deep_fusion/scheduler.py` 提供 `deep-fusion-collect` CLI，复用 serve.py 4 个采集函数调用逻辑（不改计算），serve.py 删 daemon 线程启动、去 sys.path hack、去 time.sleep 忙等。
3. P1：统一 `deep_fusion/settings.py`（pydantic-settings）收口配置；data 资产初始化脚本。
4. P1：交付物归档到 deploy/；Docker compose 拆 api + collect 两 service。
5. P1：重写 README；uv sync / inspect / --http / collect 校验。

## 后端工程化重构已完成（8-29）
- **P0 版本/依赖**：`pyproject` requires-python `>=3.14`→`>=3.11`；Dockerfile `python:3.14-slim`→`python:3.12-slim` + 修 `COPY deep_fusion/`（原错写 `DeepFusion/deep_fusion/`）；依赖分组：pytest/pandas-stubs/scipy-stubs 等移入 `[dependency-groups] dev`，pandas/fastapi/uvicorn 等放宽版本。
- **P0 调度器解耦**：新建 `deep_fusion/scheduler.py`（`deep-fusion-collect` CLI，`--kind warmup|policy|daily|report|all`），把原 serve.py 4 个 `while True` daemon 线程（缓存预热/政策采集/行业市场采集/日报）抽为单次执行函数，**核心计算逻辑零改动**。serve.py 删除 4 个线程定义 + 启动 + `sys.path.insert` hack + 无用 import，仅保留纯 API 启动（端口/主机读 env）。`deep_fusion/__init__.py` 的 `main()` 本就是干净 MCP 引擎入口（不含线程），维持。
- **P1 可复现**：`.gitignore` 调整 `data/*.db` 忽略但 `data/*.json`（校准文件）入库；新增 `data/README.md` + `scripts/init_data.py`（克隆后初始化缺失本地库）；根目录散落交付物（cloudflared/registry_add.bat/restart_all.sh/server.json/smithery.yaml）归档到 `deploy/` + `deploy/README.md`。
- **P1 部署**：`docker-compose.yml` 拆为 `deep-fusion`（API 常驻 5173）+ `collector`（deep-fusion-collect sidecar，单次执行，独立卷避免多实例抢写 data db）。
- **P1 文档**：重写根 `README.md` 定位为独立核心引擎项目，更新架构图（去 daemon 线程）、Python 版本、采集解耦说明、§9 补 `deep-fusion-collect`/`docker compose run collector`/`init_data.py`。
- **校验**：`python3 -m py_compile` 三个文件 COMPILE_OK；scheduler.py 引用的 18 个业务符号全部确认存在。
- **跳过项（避免过度工程）**：未新增 `settings.py`（pydantic-settings 已加依赖，但现有 `load_dotenv()`+env 读取已够用，非当务之急）。
- **未动**：所有 @mcp.tool 计算逻辑、两个前端、desktop panel。

## 三模块独立拆分（8-29 下午，用户"直接开三个新的"）
- **动机**：旧 `DeepFusion/` 仓库太乱（100G logs + 11993 文件大杂烩，啥都塞进去了），用户要求新建三个干净模块。
- **新建（均位于 `/home/AI/workspace/Mcp Server/` 下）**：
  1. `deepfusion-server/`：后端核心。搬 `deep_fusion/`(118 py) + `serve.py` + `pyproject.toml` + `uv.lock` + `Dockerfile` + `docker-compose.yml` + `tests/` + `agents/` + `README.md` + `.env.example` + 空 `data/`。`uv sync` ✅、`deep-fusion inspect` ✅(全量 177 工具加载成功)。**修 2 处源码 import bug**：`deep_fusion/shared/utils.py` 缺 `from typing import Any`；`deep_fusion/analysis/macro/cycles/common.py` 缺 `from .engine import CycleConfig`（原仓库因 import 顺序掩盖，inspect/HTTP 全量加载才暴露）。
  2. `deepfusion-webui/`：官方 Web 研究台。从 `DeepFusion/dashboard` 白名单复制 `src`(81 文件)/`tests`/`public` + 顶层配置（不含 node_modules/dist/.idea/timestamp mjs）。`npm install` ✅、`npm run build` ✅(`dist/` 产出)。
  3. `deepfusion-desktop/`：原地重建（保留现有 git 仓库）。删 `src-tauri/target`(2.4G Rust 构建缓存)；`lib.rs` 后端路径 `spawn_backend`+`read_doc` 改读 `DEEP_FUSION_HOME` 环境变量（缺省旧 `DeepFusion` 路径，新部署应指向 `deepfusion-server`）。`npm run build`(vite) ✅ 前端 `dashboard/src` 编译通过。
- **验证全过**：server `uv sync`+`inspect`；webui `npm install`+`build`；desktop 前端 `vite build` + lib.rs 改环境变量。
- **待办/未做**：旧 `DeepFusion/` 仓库保留未删（待用户确认清理）；desktop 的 `node_modules`/`dist` 本次未强删（删除权限超时取消，无害）；server 的 HTTP 真实启动（uvicorn bind 5173）因环境拦截 curl 未做端到端联网验证，但 `inspect` 全量加载成功已证明所有 tool 模块可 import。
- **关键约束**：核心计算逻辑零改动；仅做结构/解耦/清理层面的整理。
