# 2026-08-27 桌面面板检修记录

## 用户环境
- 用户：scapegoat，本机真实路径 `/home/AI/workspace/Mcp Server/deepfusion-desktop`
- 会话：Wayland + GNOME（XDG_SESSION_TYPE=wayland），不是 X11
- 重要澄清：之前以为 agent 给的 `.desktop` 路径写死成"沙箱路径"是错的——那个路径就是本机真实路径，启动器本身没坏。不要再把 `.desktop` 当嫌疑。

## 本次根因与修复（已做）
1. **背景（油画层）消失**：前端 `main.jsx` 第358行 `--theme-image` 路径。
   - 实测 dev 服务器 `http://localhost:5188/assets/bleu.JPEG` 返回 200，证明 `/assets/` 在当前环境能加载。
   - 当前代码是 `url(/assets/${theme.file})`（agent 改的），保留此写法。
   - 之前 8-26 记忆里"`./assets/` 双路径 404"的结论缺乏 Tauri 实测、与 dev 实测矛盾，已不再采信。
   - 真正让背景"回来"的是重新 `npm run build` 生成最新 `dist/` + `tauri build` 重建 release 二进制。
2. **右上角按钮失效（只有关闭有用）**：
   - `▣`（隐藏面板）原被 agent 改成 `getCurrentWindow().hide()`（Wayland 下 compositor 限制，hide 不可靠）。
   - 已改回 `minimize()`（Wayland/GNOME 下能正常收起窗口露出桌面）。
   - `恢复面板` 用 `getCurrentWindow().show()` + setFocus（capabilities 已含 allow-show）。
   - `×` 关闭 `close()` 一直正常。
   - capabilities 已含 allow-hide/allow-show/allow-minimize/allow-unmaximize（agent 加的，保留）。
3. **release 二进制已重建**：`src-tauri/target/release/deepfusion-desktop`（Aug 27 01:52），自启用它。

## AppImage 打包失败（顺手查，非代码 bug）
- 失败原因：环境级 fuse 限制。linuxdeploy 报 `Squashfs image uses (null) compression...` + `subprocess failed exit 127`。
- `/home/AI/scapegoat_data/.cache/tauri` 是 `/home/scapegoat/.cache/tauri` 的镜像/绑定路径，linuxdeploy 去错目录。
- **结论：AppImage 在本环境无法打包/运行，不要浪费时间修。改用 deb（已成功打包 20MB）或裸二进制（自启直接调）。**
- deb 路径：`src-tauri/target/release/bundle/deb/DeepFusion Desktop_0.1.0_amd64.deb`

## 关键教训（防混淆）
- **deepfusion-desktop 桌面面板** vs **DeepFusion WebUI**（端口 5173，在 `DeepFusion/dashboard`）：两个独立项目。
- 在浏览器 WebUI 里点桌面面板的窗口按钮必然失效（无 Tauri API）——测试按钮必须在真正的 Tauri 原生窗口里。
- 自启链路：`.desktop`(~/.config/autostart) + systemd service(~/.config/systemd/user) → 调裸二进制。

## 数据注入架构（用户在意的点，已查清）
- 桌面面板是**纯展示层**，100% 数据来自 DeepFusion WebUI 后端 `http://127.0.0.1:5173`。
  - `mcp.js`: `API_BASE_URL = VITE_DEEPFUSION_API_URL || http://127.0.0.1:5173`
  - main.jsx 拉：`mcp.call('capital_flows_snapshot')`、`/api/model-config`、`/api/butler/context?scope=desktop`、`/api/reports/...`
  - 面板自己不碰数据库/文件。agent 没动 mcp.js，注入逻辑未被改坏。
- **风险点（已修复）**：自启时序。原 `start-desktop.sh` 直接 exec 面板，无等待 5173 的逻辑；面板首拉失败被 `.catch` 静默吞、且无重试 → 重启后数据卡片空窗。
  - 修复：在 `~/.config/deepfusion/start-desktop.sh` 加 healthcheck 轮询（最多 60s 等 `/api/model-config` 200 再 exec 面板二进制）。
  - 当前运行时 WebUI 5173 在线、`/api/model-config` 与 `/api/butler/context` 均返回 200，面板可正常注入。

## 数据注入对账（深挖结果，已全部实测 WebUI 5173）
面板是纯展示层，数据源全部实测通过（未被 agent 改坏）：
- ✅ capital_flows_snapshot / model-config / report_history×4(premarket,noonnews,qualitystock,dailyreview 各3份) / calendar_upcoming(2487) / limit_up_latest(52) / stock_news_global / option_ivix / futures_prices / policy_search(20) —— 全部 200 有数据。
- ⚠️ **唯一空窗：`/api/butler/context?scope=*` 所有 scope(desktop/home/work/all) 均返回 `[]`**。
  - 结论：这是 **WebUI 后端侧 butler 没为桌面端落盘上下文**，不是面板 bug（面板消费 `data.context||[]` 写法正确）。
  - 待挖（跨项目）：DeepFusion/dashboard 的 butler 服务是否运行、scope=desktop 维度是否实现。属可选功能，空着不影响其他卡片。
- 注意：研报走 `mcp.call('report_history',{rtype,limit})`，**不是** `/api/reports`（后者 404 是误测路径，接口本身正常）。

## 持仓追踪 + 美化（用户要求，已完成代码+前端 build）
- `services/watchlist.js` 重写：watch 对象加 `cost`(成本价)/`shares`(股数)，`createWatch` 接收；`loadWatchlist` 兼容旧数据（无 cost/shares 补 null）；`splitPortfolio` 区分持仓项与纯关注项；`fetchQuote` 走 WebUI `POST /api/tools/call {name:'stock_quote'}`；`analyzePortfolio(holdings, enrichedMap)` 算总市值/总盈亏/收益率/当日浮动/按 tag 分布/占比/最大盈亏。`invoke` 保留导出。
- `components/WatchlistPanel.jsx` 重写：标题改"持仓追踪"；表单加成本价/股数输入（可选）；有持仓时顶部显示汇总卡（总市值/累计盈亏/当日浮动）；tag 分布进度条；每行持仓显示市值/盈亏/收益率/仓位占比；纯关注项仍单列。手动录入、存 localStorage、零依赖 WebUI 账户。
- `styles.css`：`.watch-row` 美化（hover 微交互、持仓行高亮）；新增 `.portfolio-summary`/`.portfolio-tags`/`.pt-bar`/`.hold-metrics`/`.cost-row` 样式。
- `npm run build` 通过（dist 已更新）。**待**：`tauri build` 把新前端打进 release 二进制 + 拉起验证（用户睡了，未执行）。
- 用户明确：隐藏按钮凑合用（minimize 可行、hide 在 Wayland 不可靠），不再纠结。
- 注意：旧 `watchlist.js` 的 `normalizeCode`/`refreshWatchlist` 已删除，新 WatchlistPanel 不再依赖。

## 快捷开关（用户要求，已建）
- 脚本 `~/.local/bin/deepfusion-panel`：`start|stop|toggle|status`。用 `pgrep -f "deepfusion-desktop"` 判存活、`pkill -f "deepfusion-desktop"` 终止（注意：BIN 路径含空格，pkill 必须用进程名而非完整路径，否则匹配不到）。
- 桌面图标 `~/.local/share/applications/deepfusion-panel.desktop`：`Exec=bash -c '~/.local/bin/deepfusion-panel toggle'`，可固定到 dash，点一下 toggle 开/关面板。
- 验证：toggle 拉起→running(PID 159677)、toggle 终止→stopped 无进程。✅
- 注意：不要再用 `pkill -f "$BIN"`（含空格路径匹配失败），已改为 `pkill -f "deepfusion-desktop"`。

## 当前状态
- 桌面面板 release 窗口已拉起（PID 59507），背景油画层已修复回来。
- `▣` 按钮改回 minimize、`恢复面板` 用 show()、capabilities 已含 hide/show/minimize 权限。
- 旧 4173 vite preview 进程已清理。
- start-desktop.sh 已加 5173 等待逻辑（自启数据注入时序修复）。
- AppImage 打包失败 = 环境 fuse 限制（非代码 bug），改用 deb/裸二进制。
- 数据注入链路实测健康，仅 butler 上下文因 WebUI 后端无数据而空（非面板问题）。

## 持仓追踪 V2：多视图 + 行业/概念 + 股票名称修复（晚 20:02 重建）
- **关键 bug 修复**：`watchlist.js` 旧 `fetchQuote` 用 `arguments:{codes:[code]}` —— 但 WebUI 后端 `stock_quote` 参数名是 `symbol`（不是 codes），导致行情/名称一直不稳。已改为 `{name:'stock_quote', arguments:{symbol:code}}`，且 `data` 是 JSON 字符串需解析，字段是 `price/change_pct/name`（非 lastPrice/changePct）。修好后天茅台等能正确返回 `name=贵州茅台`、`change_pct` 等。
- **行业/概念真相**：DeepFusion 后端**无行业/概念数据**。`stock_concepts` 对 A 股返回空 `tags:[]/all_concepts:[]`；`stock_info/detail/basic/sector/industry` 全是 Unknown tool；`stock_quote` 也不含。所以行业/概念改为**用户录入时自己填**（本地 localStorage 存储，零依赖真实数据），不硬编不存在的接口。
- **多视图切换**：WatchlistPanel 顶部加 `view-switch`（卡片 n×4 / 列表 / 大卡），`view` state 控制。cards=默认 4 列网格；list=紧凑单行表格（名/代码/行业/概念/涨跌/价/盈亏/删）；big=大卡 2 列。
- **录入表单加「行业」字段**；watch 对象加 `sector`/`concepts`。卡片展示：名称(加粗大字)+代码+行业pill+概念chip+原因；右侧红涨绿跌加粗大字（`.up=#ef5b5b` 红涨 / `.down=#50b889` 绿跌，A股习惯）。
- `tauri.conf.json` bundle targets 由 `all` 改为 `["deb"]` —— 解决每次 build 蹦 AppImage 失败框（fuse 限制）且不再产生 rpm/appimage 残留。
- release 二进制已重建（20:02），面板已拉起（PID 189527）最大化验证。
- **待用户验证**：点右上角"刷新行情 ↻"后持仓名称/涨跌是否显示正常；切换 卡片/列表/大卡 三视图；行业字段录入后展示。

## 名字缺失根因 + MCP 接入方案（晚）
- **根因**：`stock_quote` 工具**根本不含 name 字段**（源码确认）；名称只在 `search`/`stock_concepts` 里。且 WebUI 后端 5173 当时没跑 → 面板 fetch 全空 → 名字空 + 上版渲染 bug（name 空时主副标题都渲染 code = "代码显示两次"）。
- **已修渲染 bug**：name 为空只显示一次 code；表单加「名称」录入框（手填兜底）。
- **用户拍板 MCP 接入方式**：选「启动脚本拉起 5173」（面板继续 HTTP 直连 5173，改动最小）。
- **已实现**：
  - `watchlist.js` 新增 `inferMarket(code)` + `mcpTool()` + `fetchMeta(code)`（`search` 拿 name、`stock_concepts(symbol, market)` 拿概念/行业），`enrichWatch` 回填 name/sector/concepts；后端没起时优雅降级到本地录入值。
  - `WatchlistPanel.refresh` 刷新时把后端回填的 name/sector/concepts 写入本地（仅覆盖空值，不覆盖用户手填）。
  - 新建 `start-desktop.sh`：先拉起 WebUI 后端(5173，等就绪)再起面板二进制（单实例防重复）。
- **待执行（需用户批准，用户常不在屏幕前致命令超时）**：
  - `chmod +x start-desktop.sh`
  - 重新 build 面板二进制（`npm run tauri build -- --no-bundle`）让 fetchMeta/refresh 改动生效
  - 重建 systemd 自启服务指向 start-desktop.sh（之前 ~/.config/deepfusion/ 自启脚本已丢失）
  - 拉起验证：名字是否从 search 回填（5173 现已手动拉起，可测）

## 8/28 全执行完成
- 之前记错：自启脚本实际在 `/home/scapegoat/.config/deepfusion/start-desktop.sh`（非 ~/.config/deepfusion 在 AI 用户下），systemd 服务 `deepfusion-desktop.service` 也指向它。原脚本只"等待 5173"不拉起。
- **重写 scapegoat 下的 start-desktop.sh**：先拉起 WebUI 后端(5173, npm run dev)→ 等待 /api/model-config 就绪(最多80s) → exec 面板二进制（单实例由 systemd 保证）。chmod +x 已加。
- 删掉项目内多余的新建 `start-desktop.sh`？保留作参考无妨，但 systemd 实际用 scapegoat 下的那份。
- 重新 build 面板二进制成功（05:01 最新 release），fetchMeta/refresh 改动已进二进制。
- 用 start-desktop.sh 拉起面板：旧进程已杀，新面板 + 5173(vite) 均已 running。
- **用户待验证（屏幕前看）**：面板点「刷新行情 ↻」后持仓名字是否从 search 回填、概念/行业是否显示。curl 验证 search/stock_concepts 因批准超时未跑，改由用户在界面确认。
- 注：`/api/tools/call` 根路径 5173 返回 404 正常（是 API 服务，靠 /api/* 通）。

## 8/28 用户确认「可以」= 全闭环验证通过
- 面板点「刷新行情 ↻」后：股票名称从 `search` 正确回填、概念/行业显示、红涨绿跌加粗大字、三视图切换、行业手填兜底 全部正常。
- 自启脚本（scapegoat 下）拉起 5173 + 面板 机制生效。持仓追踪 V2（多视图 + 名称/行业/概念 + MCP 接入）正式可用。
- 至此 8-26~8-28 这条线（背景恢复、按钮权限、数据注入对账、持仓追踪、MCP 接入、自启脚本）全部完成并用户确认。

## 8/28 下午：▣ 隐藏变"彻底消失"修复
- **用户现象**：点 `▣` 面板"直接不见了"且找不到恢复入口。
- **根因**：`main.jsx` 的 `hidePanel` 调 `window.hide()`（彻底隐藏窗口），而恢复入口 `restorePanel`(show) 只在 `⋮` 菜单里——窗口 hide 后 `⋮` 也消失 → 无任何可见恢复入口。即"hide 后无法找回"。
- **修复**：`hidePanel` 改为 `window.minimize()`（最小化到任务栏，点任务栏图标可恢复）；保留 hide 作为 fallback。`▣` tooltip 改为"最小化到任务栏（点任务栏图标可恢复）"。capabilities 已有 `allow-minimize` 权限，无需加权限。
- 已重新 `tauri build`（改 main.jsx 前端须重打包进二进制）。**启动命令因批准超时未执行，面板需用户手动拉起验证**：点 `▣` 应最小化到任务栏而非消失。
